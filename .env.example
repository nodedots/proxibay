# Copy to .env and fill in (never commit real values).
# Base URL of the Stackduck API (NestJS). Local dev default is http://localhost:3001.
VITE_API_BASE=http://localhost:3001
# Cloud Functions base is no longer used (pre-cutover fallback, kept for reference):
# VITE_FUNCTIONS_BASE=http://localhost:5001/YOUR_FIREBASE_PROJECT_ID/europe-west1/api
